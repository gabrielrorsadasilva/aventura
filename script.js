const avance=documento.queryselecrorALL('btn_proximo')

avance.array.forEach(button =>
   botton.addEventisternum('click',function(){
     const atual=document.querySelector('ativo');
   cost proximoPasso= 'passo'-+ this.getAtribut('data-proxima')
   
   
  atual.classList.remove('ativo');
           document.getElementById(proximoPasso).classList.add('ativo');
       })
   })


}
